#!/usr/bin/env bash
# Quotr Database Setup Guide
# Apply these migrations to your Supabase project

set -e

SUPABASE_URL="${VITE_SUPABASE_URL}"
SUPABASE_ANON_KEY="${VITE_SUPABASE_ANON_KEY}"

echo "═══════════════════════════════════════════════════════════════"
echo "Quotr Database Setup"
echo "═══════════════════════════════════════════════════════════════"
echo ""
echo "This script will set up the Quotr database schema and policies."
echo ""
echo "Prerequisites:"
echo "  • Supabase project created at https://supabase.com"
echo "  • .env file with VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY"
echo ""
echo "Setup methods (choose one):"
echo ""
echo "METHOD 1: Supabase Dashboard (Recommended for first setup)"
echo "───────────────────────────────────────────────────────────────"
echo "1. Go to https://app.supabase.com"
echo "2. Open your project"
echo "3. Go to SQL Editor"
echo "4. Click 'New Query'"
echo "5. Copy the contents of supabase/migrations/001_initial_schema.sql"
echo "6. Paste into the query editor and click 'Run'"
echo "7. Repeat for supabase/migrations/002_storage_policies.sql"
echo ""
echo "METHOD 2: Supabase CLI"
echo "───────────────────────────────────────────────────────────────"
echo "1. Install Supabase CLI: npm install -g supabase"
echo "2. Login: supabase login"
echo "3. Link project: supabase link --project-ref <project-id>"
echo "4. Apply migrations: supabase db push"
echo ""
echo "METHOD 3: Direct SQL via cURL"
echo "───────────────────────────────────────────────────────────────"
echo "Run the commands in supabase/setup-db.sh (coming soon)"
echo ""
echo "═══════════════════════════════════════════════════════════════"
echo "IMPORTANT: Storage Buckets"
echo "═══════════════════════════════════════════════════════════════"
echo ""
echo "Create these storage buckets in the Supabase dashboard:"
echo "  • business-assets  (for business logos)"
echo "  • signatures       (for client digital signatures)"
echo "  • quote-pdfs       (for generated quote PDFs)"
echo ""
echo "Then apply supabase/migrations/002_storage_policies.sql"
echo ""
echo "═══════════════════════════════════════════════════════════════"
echo "VERIFICATION"
echo "═══════════════════════════════════════════════════════════════"
echo ""
echo "After setup, verify in Supabase dashboard:"
echo "  ✓ Tables exist: businesses, clients, quotes, invoices, etc."
echo "  ✓ RLS is enabled on all tables"
echo "  ✓ Trigger 'on_auth_user_created' exists"
echo "  ✓ Storage buckets created with policies"
echo ""
echo "Test by signing up in the app — a new business record should"
echo "be created automatically with subscription_status='trial'."
echo ""
